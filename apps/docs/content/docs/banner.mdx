---
title: Banner
description: A banner is a full-width component that can be used to show a message and action to the user.
icon: CircleAlert
---

<PoweredBy packages={[
  { name: 'Lucide', url: 'https://lucide.dev/' },
]} />

<Preview name="banner" code={`'use client';

import { Banner, BannerClose, BannerIcon, BannerTitle, BannerAction } from '@/components/ui/kibo-ui/banner';
import { CircleAlert } from 'lucide-react';

const Example = () => (
  <div className="flex w-full h-screen items-center justify-center">
    <Banner>
      <BannerIcon icon={CircleAlert} />
      <BannerTitle>Important message</BannerTitle>
      <BannerAction>Learn more</BannerAction>
      <BannerClose />
    </Banner>
  </div>
);

export default Example;`} />

## Installation

<Installer packageName="banner" />

## Features

- Controlled and uncontrolled visibility state
- Customizable close handler
- Optional inset variant with rounded corners
- Composable API with Icon, Title, Action and Close components
- Customizable via className prop
- Adapts to primary color tokens


## Examples

### Inset

<Preview name="banner" code={`'use client';

import { Banner, BannerClose, BannerIcon, BannerTitle, BannerAction } from '@/components/ui/kibo-ui/banner';
import { CircleAlert } from 'lucide-react';

const Example = () => (
  <div className="flex w-full h-screen items-center justify-center p-8">
    <Banner inset>
      <BannerIcon icon={CircleAlert} />
      <BannerTitle>Important message</BannerTitle>
      <BannerAction>Learn more</BannerAction>
      <BannerClose />
    </Banner>
  </div>
);

export default Example;`} />

### Different primary colors

<Preview name="banner" code={`'use client';

import { Banner, BannerClose, BannerIcon, BannerTitle, BannerAction } from '@/components/ui/kibo-ui/banner';
import { CircleAlert } from 'lucide-react';

const Example = () => (
  <div className="flex w-full h-screen items-center justify-center flex-col gap-4">
    <div className="w-full" style={{
      '--primary': '0 72.2% 50.6%',
      '--primary-foreground': '0 85.7% 97.3%',
    }}>
      <Banner>
        <BannerIcon icon={CircleAlert} />
        <BannerTitle>Something's gone <strong>horribly</strong> wrong.</BannerTitle>
        <BannerAction>Restart</BannerAction>
        <BannerClose />
      </Banner>
    </div>
    <div className="w-full" style={{
      '--primary': '24.6 95% 53.1%',
      '--primary-foreground': '60 9.1% 97.8%',
    }}>
      <Banner>
        <BannerIcon icon={CircleAlert} />
        <BannerTitle>You're almost out of disk space.</BannerTitle>
        <BannerAction>Upgrade</BannerAction>
        <BannerClose />
      </Banner>
    </div>
    <div className="w-full" style={{
      '--primary': '142.1 76.2% 36.3%',
      '--primary-foreground': '355.7 100% 97.3%',
    }}>
      <Banner>
        <BannerIcon icon={CircleAlert} />
        <BannerTitle>You've been selected for a secret mission.</BannerTitle>
        <BannerAction>Accept</BannerAction>
        <BannerClose />
      </Banner>
    </div>
  </div>
);

export default Example;`} />

## Props

The Banner component is made up of the following subcomponents:

### Banner

The `Banner` component is the main component that displays the banner.

<AutoTypeTable path="node_modules/@repo/banner/index.tsx" name="BannerProps" />

### BannerIcon

The `BannerIcon` component is used to display the icon of the banner.

<AutoTypeTable path="node_modules/@repo/banner/index.tsx" name="BannerIconProps" />

### BannerTitle

The `BannerTitle` component is used to display the title of the banner.

<AutoTypeTable path="node_modules/@repo/banner/index.tsx" name="BannerTitleProps" />

### BannerAction

The `BannerAction` component is used to display the action of the banner.

<AutoTypeTable path="node_modules/@repo/banner/index.tsx" name="BannerActionProps" />

### BannerClose

The `BannerClose` component is used to close the banner.

<AutoTypeTable path="node_modules/@repo/banner/index.tsx" name="BannerCloseProps" />
