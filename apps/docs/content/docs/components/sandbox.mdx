---
title: Sandbox
description: The sandbox component allows you to preview and test components in a sandboxed environment.
icon: Codesandbox
---

<PoweredBy packages={[
  { name: 'Sandpack', url: 'https://sandpack.codesandbox.io/' },
]} />

<Preview path="sandbox" />

## Installation

<Installer packageName="sandbox" />

## Features

- Interactive code editor with syntax highlighting and auto-completion
- Live preview of your code changes
- Built-in console for debugging
- File explorer for managing multiple files
- Resizable panels for customizing your workspace
- Tabs for switching between code, preview, and console views
- Support for multiple file types
- Real-time error reporting

## Examples

### Without File Explorer

<Preview path="sandbox-no-file-explorer" />

## Props

The Sandbox component is made up of the following subcomponents:

### SandboxProvider

The `SandboxProvider` component is used to provide the features to the sandbox.

<AutoTypeTable path="node_modules/@repo/sandbox/index.tsx" name="SandboxProviderProps" />

### SandboxLayout

The `SandboxLayout` component is used to display the layout of the sandbox.

<AutoTypeTable path="node_modules/@repo/sandbox/index.tsx" name="SandboxLayoutProps" />

### SandboxTabs

The `SandboxTabs` component is used to display the tabs of the sandbox.

<AutoTypeTable path="node_modules/@repo/sandbox/index.tsx" name="SandboxTabsProps" />

### SandboxTabsList

The `SandboxTabsList` component is used to display the list of tabs.

<AutoTypeTable path="node_modules/@repo/sandbox/index.tsx" name="SandboxTabsListProps" />

### SandboxTabsTrigger

The `SandboxTabsTrigger` component is used to display a tab.

<AutoTypeTable path="node_modules/@repo/sandbox/index.tsx" name="SandboxTabsTriggerProps" />

### SandboxTabsContent

The `SandboxTabsContent` component is used to display the content of a tab.

<AutoTypeTable path="node_modules/@repo/sandbox/index.tsx" name="SandboxTabsContentProps" />

### SandboxCodeEditor

The `SandboxCodeEditor` component is used to display the code editor of the sandbox.

<AutoTypeTable path="node_modules/@repo/sandbox/index.tsx" name="SandboxCodeEditorProps" />

### SandboxPreview

The `SandboxPreview` component is used to display the preview of the sandbox.

<AutoTypeTable path="node_modules/@repo/sandbox/index.tsx" name="SandboxPreviewProps" />

### SandboxConsole

The `SandboxConsole` component is used to display the console of the sandbox.

<AutoTypeTable path="node_modules/@repo/sandbox/index.tsx" name="SandboxConsoleProps" />

### SandboxFileExplorer

The `SandboxFileExplorer` component is used to display the file explorer of the sandbox.

<AutoTypeTable path="node_modules/@repo/sandbox/index.tsx" name="SandboxFileExplorerProps" />
